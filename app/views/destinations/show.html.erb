<p>
	<strong>
		Latitude and Longitude:
	</strong>
	<%= @destination.latitude.to_s + ", " + @destination.longitude.to_s %>
</p>

<%= javascript_tag do %>
	var latitude = '<%= @destination.latitude.to_s %>'
	var longitude = '<%= @destination.longitude.to_s %>'
	var address = '<%= @destination.city.to_s %>'
<% end %>

<div id="destination-map"></div>